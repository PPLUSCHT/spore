import { CommentTypeDirective } from './comment-type-directive.directive';

describe('CommentTypeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentTypeDirective();
    expect(directive).toBeTruthy();
  });
});
