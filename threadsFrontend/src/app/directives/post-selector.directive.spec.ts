import { PostSelectorDirective } from './post-selector.directive';

describe('PostSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new PostSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
